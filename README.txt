1. Go to the application root folder and run `npm install` command.
2. In order to run the application, run `node server.js` command.
3. To test all the API's, Open Postman and import the `Contacts-API.postman_collection` 
file which is given in the application root folder. 